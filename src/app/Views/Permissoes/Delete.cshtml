@model app.Models.Entities.Permissao

@{
    Layout = "_ContentLayout";
    ViewData["Title"] = "Excluir Permissão";
}

<h1 class="mb-4">Excluir Permissão</h1>

<div style="max-width: 400px;">
    <h3>Tem certeza que deseja excluir esta permissão?</h3>
    
    <div class="mb-3">
        <label class="form-label">Nome</label>
        <input class="form-control" value="@Model.Nome" readonly />
    </div>
    
    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="Id" />
        <div class="d-flex gap-2">
            <button type="submit" class="btn btn-danger w-100">Excluir</button>
            <a asp-action="Index" class="btn btn-secondary w-100">Voltar</a>
        </div>
    </form>
</div>
